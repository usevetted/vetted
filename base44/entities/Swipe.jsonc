{
  "name": "Swipe",
  "type": "object",
  "properties": {
    "swiper_profile_id": {
      "type": "string"
    },
    "target_profile_id": {
      "type": "string"
    },
    "target_type": {
      "type": "string",
      "enum": [
        "job",
        "candidate"
      ]
    },
    "action": {
      "type": "string",
      "enum": [
        "pass",
        "like",
        "super"
      ]
    },
    "context_job_id": {
      "type": "string"
    }
  },
  "required": [
    "swiper_profile_id",
    "target_profile_id",
    "action"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "created_by_id": {
        "$exists": true
      }
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}